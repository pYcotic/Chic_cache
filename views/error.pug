extends layout

block content
  //h1= message
  //h2= error.status
  //pre #{error.stack}

  if error
    h1= message
    h2= error.status
    pre #{error.stack}
  else
    p An error occurred, but no further information is available.
